1) @FindBy(id="A")
private WebElement a;
取单个元素
2）@FindBys（
{
  @FindBy(classname = "A")
  @FindBy(xpath = "[//@name=""]")
}
）
private WebElement 
取交集，类似于
drviver.findelement(by.classname='A').findelement.(by.xpath).

@FindAll

@FindAll({
    @FindBy(id = "A"),
    @FindBy(id = "B")
})
public List<WebElement> aAndB;
@FindAll相当于是取并集，如找到id=A和id=B的所有元素