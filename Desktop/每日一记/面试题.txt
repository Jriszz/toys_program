用python写了一个模块，即.py文件，如何让你的工程找到它，哪种方式最好

1.函数添加
1,import sys
2.sys.path
3.sys.path.append("c:\\")
2.修改环境变量
用户修改环境变量PYTHONPATH
3.增加.pth文件，推荐

在python安装根目录添加一个路径文件，如mypkpath.pth,必须以.pth为后缀，写上要加上的模块的文件所在的目录名称


7.Java问题，hashSet,hashMap,hashTable的区别。java序列和反序列的意思

8.进程与线程的区别，进程同步，互斥，通信
进程同步：为了解决进程间松散的协作关系( 直接制约关系)而引入进程同步；
资源竞争出现了两个控制问题：
一个是死锁 （deadlock ）问题，一组进程如果都获得了部分资源，
还想要得到其他进程所占有的资源，最终所有的进程将陷入死锁。
另一个是饥饿（starvation ）问题，
这是指这样一种情况：一个进程由于其他进程总是优先于它而被无限期拖延。

某些进程为完成同一任务需要分工协作，由于合作的每一个进程都是独立地以不可预知的速度推进，这就需要相互协作的进程在某些协调点上协 调各自的工作。
这种协作进程之间相互等待对方消息或信号的协调关系称为进程同步。


进程互斥：为了解决进程间竞争关系（间接制约关系）而引入进程互斥

进程通信：为了解决进程间紧密的协作关系而引入进程通信。

信号量是进程同步与互斥的常用方法，也可以作为低级的进程通信方法，用于传递控制信号。

线程同步是多个线程同时访问同一资源，等待资源访问结束，浪费时间，效率低    线程异步：访问资源时在空闲等待时同时访问其他资源，实现多线程机制

进程是资源的分配和调度的一个独立单元，而线程是CPU调度的基本单元

线程可能是一组指令集合，线程通常指一个程序

异步与同步：
同步方法一旦开始，调用者必须等到方法调用返回之后，才能继续后续行为
无先后顺序，一旦开始，方法调用便立即返回，调用者就可以继续后续行为，一般为另一个线程执行